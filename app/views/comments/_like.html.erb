<% if like_comment_ids.include?(comment.id) %>
  <%= link_to '♥', comment_likes_path(comment), method: :delete, remote: true %>
<% else %>
  <%= link_to '♡', likes_comment_path(comment), method: :post, remote: true %>
<% end %>
